# Technical steering

- Product runtime: Node.js 22, ECMAScript modules, zero runtime dependencies.
- Tests: Node built-in test runner.
- Knowledge root: `.ua/`.
- UA runtime: vendored upstream source.
- Task outputs: one self-contained folder per task.
- External MCP services are not required.
